<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Egems" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
    <link rel="stylesheet" type="text/css" media="screen" href="http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.14/themes/base/jquery-ui.css">
  </head>
  <body>

    <div class="container-fluid">
      <!-- ========== MAIN TOP NAV ========== -->
      <div id="main-nav" class="navbar navbar-fixed-top">
        <div class="navbar-inner">
          <div class="container">
            <a class="brand" href="#">Egems</a>

            <button class="btn btn-navbar">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>

            <div class="nav-collapse collapse">
              <% current_uri = request.env['PATH_INFO'] %>
              <ul id="navs-to-pages" class="nav">
                <%= timesheet_top_nav(current_user, current_uri) %>
                <%= overtime_top_nav(current_user, current_uri) %>
                <%= leave_details_top_nav(current_user, current_uri) %>
                <%= hr_module_top_nav(current_user) %>
              </ul>
              <ul id="user-control-nav" class="nav pull-right">
                <li>
                  <a href="#">
                    <i class="icon-user"></i> <%= current_user.employee.full_name %>
                  </a>
                </li>
                <%= render partial: "layouts/notification" unless leave_unit_sum.zero? || leave_unit_sum.nil? %>
                <li class="divider-vertical"></li>
                <li>
                <%= link_to "<i class=\"icon-signout\"></i> Signout".html_safe,
                  signout_path, :method => :delete %>
                </li>
              </ul>
            </div>
          </div>
        </div>
        <!-- ========== SUB NAV ========== -->
        <div class="navbar-sub">
          <div class="container">
            <h2 id="digiclock" class="nav pull-right" href="#">
              <i class="icon-time"></i>
              <span id="time" data-time="<%= now = Time.now %>">
                <%= format_long_date_with_time(now) %>
              </span>
            </h2>
          </div>
        </div>
      </div>

      <div class="container">
        <%= content_tag "div", "", :id => "data-container", :data => @data %>
        <div id="main-container" style="min-height:100px; min-width:724px;">
          <div class="modal">
            <div class="modal-body" style="padding:0; margin:0">
              <div class="progress progress-striped active" style="padding:0; margin:0; height:25px;">
                <div class="bar" style="width:100%; height:100%;"><i>Loading...</i></div>
              </div>
            </div>
          </div>
        </div>
        <footer class="footer">
            <p>&copy; Egems 2012</p>
        </footer>
      </div>

    </div> <!-- /container -->
    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.14/jquery-ui.min.js"></script>
    <div id="loading-indicator" class="modal hide">
      <div class="modal-body" style="padding:0; margin:0">
        <div class="progress progress-striped active" style="padding:0; margin:0; height:25px;">
          <div class="bar" style="width:100%; height:100%;"><i>Loading...</i></div>
        </div>
      </div>
    </div>
  </body>
</html>
